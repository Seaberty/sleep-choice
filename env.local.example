# Deprecated duplicate — use the dotfile template instead:
#   See .env.local.example (committed) and copy it to .env.local
#
# Do not put real API keys in any committed *.example file.
# If secrets were ever committed, rotate keys in Google AI Studio, Serper, Supabase, DeepSeek.
